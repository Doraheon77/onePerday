# 프로젝트 요약: 영양제 라벨 인식 (TabletYOLO)

## 1. 프로젝트 목표
영양제 라벨 OCR 전처리를 위한 **1-class supplement_label 검출 모델** 구축. 
- 클래스: `supplement_label` (1개)
- 목적: 제품별 분류가 아닌, OCR을 위한 **crop-safe bbox** 검출.

## 2. 데이터셋 정보 (26-04-27 기준)
- **데이터 구성:** 총 61장 (train 43, val 12, test 6)
- **라벨링:** YOLO 포맷 (.txt) 사용, 이미지와 파일명 일치.
- **데이터 처리:**
  - 이미지 크기 정보(width/height)가 누락된 XML 파일 복구 완료.
  - Pascal VOC(XML)를 YOLO 포맷(TXT)으로 변환 완료.

## 3. 학습 및 모델 정보
- **모델:** YOLO26s (`yolo26s.pt`)
- **최적화 전략:**
  - `MuSGD` 옵티마이저 사용.
  - 과적합 방지를 위해 백본 레이어 10개 고정 (`freeze=10`).
  - 소량 데이터 특성을 고려하여 `mosaic=0.0` 설정.
- **결과:** mAP50 0.975 수준의 우수한 성능 달성.

## 4. 현황 및 성능 분석
- **성능:** 일반적인 원통형 약통 라벨에서는 매우 높은 정확도로 탐지 성공.
- **한계점:** 
  - **소형 객체 탐지:** 극단적으로 작게 촬영된 라벨은 탐지율이 떨어짐.
  - **캘리브레이션 의존성:** 모델 자체의 검출 성능 외에도, 사용자가 촬영 시 병과 카메라의 거리/각도를 조정하는 **카메라 앱 내 사용자 캘리브레이션/가이드**가 필수적인 보완책으로 확인됨.

## 5. 향후 과제
- 실제 실사용 환경에서 특정 각도나 조명 실패 사례를 수집하여 추가 학습(Fine-tuning) 진행.
- 카메라 앱 내 촬영 가이드라인(거리 유지 등) 기능 연동 필요.
- Crop 결과물의 해상도 및 왜곡 보정을 위한 후처리 OCR 파이프라인 연동.
