> **목표:** “영양제 라벨의 정확한 외곽선”을 찾는 것이 아니라, **OCR이 읽어야 할 라벨 영역을 누락 없이 포함하는 crop-safe bbox**를 학습한다.

bbox만 사용할 예정이라면 데이터셋 구축 목표를 다음처럼 잡는 것이 좋습니다.
즉, bbox는 조금 넓어도 괜찮지만 **라벨 일부가 잘리면 안 됩니다.**

---

# 1. 클래스 설계

제품별 클래스가 아니라 **1-class**로 구성하는 것이 가장 적절합니다.

```yaml
names:
  0: supplement_label
```

비추천:

```yaml
0: aronamin_gold_premium
1: centrum
2: omega3
3: vitamin_c
```

이 프로젝트에서 YOLO의 역할은 “아로나민 골드 프리미엄”을 분류하는 것이 아니라, **OCR 전에 읽을 라벨 영역을 잘라내는 것**입니다. 제품명 판별은 후속 OCR + LLM + DB 매칭에서 처리하는 편이 맞습니다.

Ultralytics detection 데이터셋은 이미지별 `.txt` 라벨 파일에 `class x_center y_center width height` 형식의 정규화된 xywh 좌표를 저장하는 구조입니다. ([Ultralytics Docs][1])

---

# 2. 데이터셋 기본 구조

```text
supplement_label_dataset/
  images/
    train/
    val/
    test/
  labels/
    train/
    val/
    test/
  supplement_label.yaml
```

`data.yaml` 예시:

```yaml
path: ./supplement_label_dataset

train: images/train
val: images/val
test: images/test

names:
  0: supplement_label
```

라벨 파일 예시:

```txt
0 0.5123 0.4875 0.6240 0.3180
```

좌표는 픽셀값이 아니라 이미지 크기 대비 0~1 정규화 값이어야 합니다. ([Ultralytics Docs][1])

---

# 3. “박스 처리한 라벨링 이미지”는 학습 이미지로 쓰면 안 됨

현재 구성 가능한 데이터가 다음과 같다고 했습니다.

```text
원본 이미지
라벨지를 박스 처리한 라벨링 이미지
```

이때 **박스가 그려진 이미지 자체를 학습 이미지로 넣으면 안 됩니다.**

모델이 영양제 라벨이 아니라, 사람이 그려 넣은 **사각형 선, 색상, 테두리 패턴**을 학습할 수 있습니다. 반드시 다음 구조로 변환해야 합니다.

```text
원본 이미지: img_001.jpg
라벨 좌표: img_001.txt
```

즉, 박스 처리 이미지는 학습용 이미지가 아니라 **annotation 추출 또는 검수용 자료**로만 사용해야 합니다.

---

# 4. bbox 라벨링 기준

bbox 학습에서는 라벨링 기준을 처음부터 고정해야 합니다. 기준이 흔들리면 50~100장 소량 데이터에서 모델이 매우 불안정해집니다.

## 권장 라벨링 기준

bbox는 다음 영역을 포함합니다.

```text
영양제 병 또는 박스 전체 X
라벨지 전체 중 OCR에 필요한 전면 라벨 영역 O
```

예를 들어 아로나민 골드 프리미엄 병이라면 다음을 포함합니다.

```text
제품명
브랜드명
주요 성분/함량
복용 관련 문구
라벨의 전면 인쇄 영역
```

다음은 제외하는 편이 좋습니다.

```text
병뚜껑
병 바닥
투명 플라스틱 여백
라벨이 아닌 배경
손가락
책상
```

다만 OCR 안정성을 위해 bbox를 너무 타이트하게 잡으면 안 됩니다.

권장 여백:

```text
좌우: 라벨 경계 기준 3~8% 여백
상하: 라벨 경계 기준 5~10% 여백
```

특히 bbox detector는 OBB처럼 회전된 라벨을 타이트하게 감싸지 못하므로, **기울어진 라벨은 축 정렬 bbox가 넓어지는 것을 허용**해야 합니다.

---

# 5. 특수 케이스 라벨링 규칙

## 5.1 라벨이 기울어진 경우

bbox는 회전된 라벨을 따라가지 말고, 축 정렬 직사각형으로 감쌉니다.

```text
기울어진 라벨 전체가 잘리지 않도록 가장 바깥쪽 x/y 좌표 기준으로 박스 생성
```

이 경우 배경이 일부 포함되는 것은 정상입니다. OCR 전처리 단계에서 perspective correction을 하지 않는다면, bbox crop 후 OCR 모델이 직접 기울어진 텍스트를 읽게 됩니다. 따라서 학습 데이터에도 이런 케이스를 충분히 넣어야 합니다.

## 5.2 라벨이 180도 뒤집힌 경우

뒤집힌 상태 그대로 bbox를 칩니다.

```text
라벨이 뒤집혀 있어도 supplement_label로 동일하게 라벨링
```

단, 후속 OCR에서는 방향 보정이 필요합니다. 데이터셋 단계에서는 뒤집힌 이미지를 별도 클래스로 나누지 않는 것이 좋습니다.

비추천:

```yaml
0: supplement_label_normal
1: supplement_label_rotated
2: supplement_label_upside_down
```

추천:

```yaml
0: supplement_label
```

## 5.3 라벨이 작게 촬영된 경우

라벨이 이미지 전체의 10~25% 정도만 차지하는 케이스를 반드시 넣어야 합니다.

실사용에서는 사용자가 병을 멀리서 찍거나, 화면 한쪽에 작게 담을 가능성이 큽니다. 이 케이스가 없으면 모델이 정면 클로즈업에서는 잘 되지만 실제 앱 촬영에서는 라벨을 놓칠 수 있습니다.

작은 라벨 대응에는 학습 이미지 크기와 scale augmentation도 중요합니다. Ultralytics 문서에서도 작은 객체 감지에는 이미지 크기 증가가 도움이 될 수 있다고 설명합니다. ([Ultralytics Docs][2])

## 5.4 라벨 일부가 가려진 경우

기준을 정해두는 것이 좋습니다.

| 상황              | 라벨링 여부           |
| --------------- | ---------------- |
| 라벨 80% 이상 보임    | 라벨링              |
| 라벨 50~80% 보임    | OCR 가능하면 라벨링     |
| 라벨 절반 이상 가려짐    | 보통 제외            |
| 제품명 영역이 완전히 가려짐 | 제외 권장            |
| 손가락이 일부 가림      | 실사용 케이스이므로 일부 포함 |

라벨이 너무 많이 가려진 이미지를 positive로 넣으면, 모델이 “읽을 수 없는 라벨”까지 잡게 됩니다. OCR 등록 기능에서는 오히려 후속 오류를 늘릴 수 있습니다.

## 5.5 한 이미지에 여러 라벨이 보이는 경우

앱의 목적이 “한 제품 등록”이라면 원칙을 이렇게 잡는 것이 좋습니다.

```text
가장 크고 정면에 가까운 주 라벨만 annotation
```

여러 제품이 동시에 찍힌 상황까지 지원하려면 모든 라벨에 bbox를 칠 수 있습니다. 하지만 50~100장에서는 task 범위를 좁히는 편이 낫습니다.

권장:

```text
초기 버전: 주 라벨 1개만 검출
고도화 버전: 이미지 내 모든 supplement_label 검출
```

---

# 6. 촬영 데이터 구성 전략

50~100장이라면 “많이”보다 “다양하게”가 중요합니다.

권장 구성은 다음과 같습니다.

| 유형                   | 권장 비율 | 100장 기준 |
| -------------------- | ----: | ------: |
| 정면 클로즈업              |   20% |     20장 |
| 약간 기울어진 라벨           |   20% |     20장 |
| 크게 기울어진 라벨           |   15% |     15장 |
| 작게 촬영된 라벨            |   20% |     20장 |
| 어두운 조명/반사/그림자        |   10% |     10장 |
| 손으로 들고 촬영            |   10% |     10장 |
| 라벨 없음/유사 배경 negative |    5% |      5장 |

50장만 만들 수 있다면 다음 정도가 현실적입니다.

| 유형       | 50장 기준 |
| -------- | -----: |
| 정면 정상    |    10장 |
| 기울어짐     |    10장 |
| 작게 촬영    |    10장 |
| 뒤집힘/회전   |     6장 |
| 반사/어두움   |     6장 |
| 손/복잡한 배경 |     5장 |
| negative |     3장 |

---

# 7. 반드시 포함해야 할 촬영 조건

## 7.1 거리 변화

라벨 크기를 다양하게 만들어야 합니다.

```text
화면의 70~90%를 차지하는 클로즈업
화면의 40~60%를 차지하는 일반 촬영
화면의 10~30%만 차지하는 원거리 촬영
```

bbox detector가 OCR crop 전처리용이면 특히 **작게 촬영된 라벨 recall**이 중요합니다.

## 7.2 각도 변화

bbox 기반이라도 각도 변화는 반드시 들어가야 합니다.

```text
0도 정면
±15도
±30도
±45도
±60도
90도 회전
180도 뒤집힘
```

Ultralytics augmentation에는 회전, 이동, 스케일, shear, perspective 같은 파라미터가 있으며, 이러한 기하 증강은 다양한 시점과 촬영 조건 대응에 사용됩니다. ([Ultralytics Docs][3])

## 7.3 조명 변화

영양제 병은 플라스틱/유광 라벨이 많아서 반사가 중요합니다.

반드시 포함:

```text
밝은 실내등
어두운 실내
스마트폰 그림자
라벨 부분 반사광
역광
흐릿한 초점
약한 모션블러
```

## 7.4 배경 변화

배경이 단순하면 실사용에서 성능이 떨어질 수 있습니다.

포함할 배경:

```text
흰 책상
나무 책상
침대/천 배경
주방/식탁
약통 여러 개가 있는 배경
손으로 들고 촬영
박스 포장지 근처
```

---

# 8. negative image 전략

negative image는 매우 중요합니다. 라벨이 없는 이미지에도 모델이 박스를 치기 시작하면 OCR 파이프라인 전체가 오작동합니다.

negative image에는 라벨 파일을 만들지 않거나, 빈 `.txt`를 둘 수 있습니다. Ultralytics detection 포맷에서는 이미지에 객체가 없으면 해당 이미지의 `.txt`가 없어도 됩니다. ([Ultralytics Docs][1])

권장 negative 예시:

```text
라벨 없는 흰색 병
물병
화장품 병
약 봉투
박스 포장
책 표지
영수증
손만 나온 사진
책상 배경만 나온 사진
흐릿해서 라벨을 읽을 수 없는 영양제 병
```

초기 50~100장에서는 negative를 너무 많이 넣을 필요는 없습니다. 전체의 5~15% 정도가 적절합니다.

---

# 9. train / val / test 분할 전략

소량 데이터에서는 랜덤 분할을 피해야 합니다.

비추천:

```text
같은 제품을 같은 장소에서 20장 연속 촬영
→ 무작위로 train/val/test 분할
```

이렇게 하면 거의 같은 이미지가 train과 val에 동시에 들어가서 검증 성능이 과대평가됩니다.

추천:

```text
제품 단위 또는 촬영 세션 단위로 분할
```

예시:

```text
train: 아로나민, 센트룸, 오메가3 일부
val: 다른 조명/다른 배경의 아로나민, 비타민C
test: 학습에 안 쓴 제품 또는 다른 촬영 날짜
```

권장 비율:

```text
train: 70%
val: 20%
test: 10%
```

100장 기준:

```text
train: 70장
val: 20장
test: 10장
```

50장 기준:

```text
train: 35장
val: 10장
test: 5장
```

단, 50장은 test가 너무 작기 때문에 가능하면 test를 고정하지 말고, 5-fold cross validation을 고려할 수 있습니다. Ultralytics도 제한된 데이터에서 K-Fold cross validation을 활용하는 가이드를 제공합니다. ([Ultralytics Docs][2])

---

# 10. augmentation 전략

bbox 기반에서는 OBB보다 회전·원근 증강을 조심해야 합니다. 기울어진 라벨을 axis-aligned bbox로 감싸면 박스가 커지고 배경이 많이 포함되기 때문입니다.

그래도 실사용 대응을 위해 다음 증강은 필요합니다.

권장 학습 증강:

```bash
degrees=180
translate=0.10~0.15
scale=0.40~0.60
shear=3~5
perspective=0.0003~0.0008
hsv_h=0.01
hsv_s=0.20~0.30
hsv_v=0.30~0.40
fliplr=0.0
flipud=0.2~0.3
mosaic=0.0~0.2
mixup=0.0
copy_paste=0.0
```

특히 주의할 점:

| 파라미터          | 권장                                |
| ------------- | --------------------------------- |
| `degrees`     | 뒤집힘 대응 때문에 크게 줘도 됨                |
| `fliplr`      | 텍스트가 좌우 반전되므로 비추천                 |
| `flipud`      | 뒤집힌 촬영 대응용으로 일부 허용                |
| `mosaic`      | OCR crop 목적에는 비현실적 장면을 만들 수 있어 낮게 |
| `mixup`       | 라벨 경계가 흐려져 비추천                    |
| `perspective` | 너무 크면 bbox가 과도하게 커지므로 약하게         |

Ultralytics fine-tuning 문서에서도 소량 데이터에서는 overfitting 방지를 위해 증강, layer freeze, 낮은 learning rate 등을 조정하는 접근을 설명합니다. ([Ultralytics Docs][2])

---

# 11. bbox 라벨링 시 권장 규칙표

라벨러에게 줄 수 있는 실무 규칙은 다음과 같습니다.

| 상황                 | 라벨링 규칙                            |
| ------------------ | --------------------------------- |
| 라벨이 정면             | 라벨 인쇄 영역 전체 + 5% 여백               |
| 라벨이 기울어짐           | 기울어진 라벨 전체를 axis-aligned bbox로 포함 |
| 라벨이 작음             | 보이는 라벨 전체를 최대한 타이트하게 포함           |
| 라벨 일부 반사           | 제품명/주요 텍스트가 보이면 라벨링               |
| 라벨 일부 가림           | OCR 가능하면 라벨링, 제품명 불가하면 제외         |
| 여러 라벨              | 초기 버전은 가장 큰 주 라벨만                 |
| 박스형 영양제 패키지        | 전면 제품 정보 영역만 bbox                 |
| 병 전체만 보이고 라벨 식별 불가 | negative 처리                       |
| 라벨 없는 유사 병         | negative 처리                       |
| 심하게 흔들린 이미지        | OCR 불가능하면 negative 또는 제외          |

---

# 12. annotation quality control

50~100장에서는 라벨링 품질이 데이터 수보다 중요합니다. 다음 검수를 반드시 하세요.

## 12.1 박스 누락 검사

전체 이미지 중 라벨이 있는데 `.txt`가 없는 파일이 있는지 확인합니다.

## 12.2 좌표 범위 검사

YOLO 좌표는 모두 0~1이어야 합니다.

```text
class x_center y_center width height
```

다음 값은 오류입니다.

```text
0 512 360 300 120
```

정상:

```text
0 0.512 0.487 0.300 0.120
```

## 12.3 과도하게 작은 박스 검사

라벨이 실제로 큰데 bbox가 너무 작으면 OCR crop에서 텍스트가 잘립니다.

## 12.4 과도하게 큰 박스 검사

병 전체나 책상까지 포함하는 박스가 많으면 OCR에 불필요한 텍스트/배경이 들어갑니다.

권장 기준:

```text
bbox는 라벨 영역보다 약간 커야 하지만,
병 전체를 감쌀 정도로 커지면 안 됨.
```

---

# 13. 앱 실사용을 고려한 데이터 수집 루틴

가장 추천하는 수집 방식은 다음입니다.

## Step 1. 제품 5~10종 준비

가능하면 다음처럼 형태가 다른 제품을 섞습니다.

```text
원통형 병
상자형 패키지
작은 병
큰 병
유광 라벨
무광 라벨
흰색 라벨
금색/은색 반사 라벨
```

## Step 2. 제품당 8~12장 촬영

각 제품마다 다음 조건을 찍습니다.

```text
1장: 정면 클로즈업
1장: 정면 일반 거리
1장: 작게 촬영
1장: 좌측 기울임
1장: 우측 기울임
1장: 위/아래 기울임
1장: 90도 회전
1장: 180도 뒤집힘
1장: 어두운 조명
1장: 반사광
1장: 손으로 들고 촬영
1장: 복잡한 배경
```

10제품 × 10장 = 100장입니다.

## Step 3. negative 5~15장 추가

```text
영양제 아닌 병
라벨 없는 병
책상
손
박스
화장품
약 봉투
```

## Step 4. train/val/test는 제품 또는 세션 기준 분리

같은 제품을 여러 각도에서 찍은 사진이 모두 train과 val에 섞이지 않도록 주의합니다.

---

# 14. bbox만 사용할 때의 현실적 한계와 보완책

bbox는 회전된 라벨을 타이트하게 자를 수 없습니다. 따라서 다음 문제가 생깁니다.

```text
기울어진 라벨 crop에 배경이 많이 포함됨
곡면 병에서는 라벨 일부가 왜곡됨
OCR 모델이 회전 텍스트를 그대로 받아야 함
```

이를 보완하려면 데이터셋 단계에서 다음을 반영해야 합니다.

```text
기울어진 이미지 충분히 포함
뒤집힌 이미지 충분히 포함
작게 촬영된 이미지 충분히 포함
반사광 이미지 포함
bbox 여백 기준 일관화
```

추론 단계에서는 다음 후처리를 권장합니다.

```text
YOLO bbox crop
→ crop에 5~10% padding 추가
→ 0/90/180/270도 OCR 시도
→ OCR confidence가 가장 높은 방향 선택
→ 제품명 후보를 DB와 fuzzy matching
```

---

# 15. 최종 권장 데이터셋 설계

가장 현실적인 구성은 다음입니다.

```text
클래스:
  supplement_label 1개

총 이미지:
  80~120장 권장
  최소 50장 가능

positive:
  70~90%

negative:
  10~15%

라벨링:
  원본 이미지에 대해서만 bbox annotation
  박스가 그려진 이미지는 학습에 사용 금지

분할:
  train 70%
  val 20%
  test 10%
  제품/촬영세션 기준 분리

bbox 기준:
  OCR에 필요한 전면 라벨 전체 + 3~10% 여백
  병 전체는 포함하지 않음
  기울어진 라벨은 axis-aligned bbox로 전체 포함

핵심 케이스:
  작게 촬영
  기울어짐
  뒤집힘
  반사광
  어두운 조명
  손으로 들고 촬영
  복잡한 배경
```

요약하면, bbox 기반 데이터셋에서는 **정확한 라벨 외곽선보다 OCR-safe crop 일관성**이 중요합니다. 50~100장 규모라면 데이터 수를 늘리는 것보다, **실제 앱에서 실패할 촬영 조건을 의도적으로 포함하고, 같은 기준으로 bbox를 치는 것**이 성능에 더 크게 영향을 줍니다.

[1]: https://docs.ultralytics.com/datasets/detect/?utm_source=chatgpt.com "Object Detection Datasets Overview"
[2]: https://docs.ultralytics.com/guides/finetuning-guide/?utm_source=chatgpt.com "How to Fine-Tune YOLO on a Custom Dataset"
[3]: https://docs.ultralytics.com/guides/yolo-data-augmentation/?utm_source=chatgpt.com "Data Augmentation using Ultralytics YOLO"
